// React Imports
import type { SVGAttributes } from 'react'

const Logo = (props: SVGAttributes<SVGElement>) => {
  return (
    <img src='/images/logos/pisa.png' width={22} height={22} alt='Logo' />
  )
}

export default Logo
